/**
 * Must know
 * 1. var, let, const (done)
 * 2. template string
 *    2.1: create multi-line string (``)
 *    2.2: add dynamic variable (expression): `${}`
 * 3. Arrow function
 *    3.1: single parameter arrow function
 *    3.2: two parameter arrow function
 *    3.3: no parameter  arrow function
 *    3.4: arrow function with function body. And don't forget you have to return explicitly
 * 4. map
 * 5. filter
 * 6. find
 * 7. optional chaining
 */

/**
 * Good to have
 * 1. default parameter
 * 2. spread operator: [...number, 12, 48]
 * 3. destructuring
 *    3.1: object destructuring: how to get properties name as variable: const{x} = {x: 3, y: 5}
 *    3.2: array destructuring: const [a, b] = [2,5,52,91]
 * 4. forEach
 * 5. class, object
 * 6. dot notation vs bracket notation 
 */

/**
 * Should know the name
 * 1. reduce
 * 2. inheritance
 * 3. prototypical chain
 */
